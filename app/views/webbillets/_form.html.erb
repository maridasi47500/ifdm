<%= form_with(model: webbillet, local: true) do |form| %>
  <% if webbillet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(webbillet.errors.count, "error") %> prohibited this webbillet from being saved:</h2>

      <ul>
        <% webbillet.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :flyer %>
    <%= form.file_field :flyer %>
  </div>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url %>
  </div>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
